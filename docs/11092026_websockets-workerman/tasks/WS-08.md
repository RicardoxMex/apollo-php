# WS-08 — Docs: docs/websockets.md + actualizar docs/realtime.md + docs/README.md

- **Estado:** PENDING (depende de WS-07)
- **Plan:** 11092026_websockets-workerman · **Hito:** M3 · **Riesgo:** LOW
- **Agente:** backend · **Skills:** —
- **Depende de:** WS-07
- **Produce:** `docs/websockets.md` (nuevo), `docs/realtime.md` (modificado), `docs/README.md` (modificado).

## Goal

Documentar el módulo WebSocket cubriendo los 14 puntos del spec §22. Actualizar `docs/realtime.md` (sin OpenSwoole, con Workerman). Enlazar desde `docs/README.md`.

## Pasos

1. `docs/websockets.md` (español, estilo `docs/realtime.md`): Instalación, Configuración, Variables de entorno, Iniciar servidor (`composer websocket`, `php apollo realtime:start`), Autenticación (JWT en handshake, en query), Crear notificaciones (`NotificationService::sendToUser`), Consumir WebSocket desde frontend (SDK), Reconexión, Desarrollo local, Producción, Nginx + WSS, Redis (estado actual + roadmap), Troubleshooting (Windows sin daemon, `count=1`, WSS, JWT inválido, etc.).
2. `docs/realtime.md`: actualizar secciones que mencionan OpenSwoole → Workerman; mantener la API `Realtime::broadcast`, `Realtime::to()->emit`, `Notification::send`; cambiar comandos a los nuevos; eliminar la nota de que "el driver `local` no comunica entre instancias" (HttpEventBus sí lo hace vía HTTP interno).
3. `docs/README.md`: enlazar `websockets.md` en el índice; el módulo "Realtime" pasa a ser "WebSockets (Workerman)" con link a `websockets.md`.

## Output Contract

Devolver: diffs, evidencia de que el manual queda coherente.
