# WS-07 — Tests + composer test verde

- **Estado:** PENDING (depende de WS-05, WS-06)
- **Plan:** 11092026_websockets-workerman · **Hito:** M3 · **Riesgo:** MEDIUM
- **Agente:** testing · **Skills:** testing/tdd
- **Depende de:** WS-05, WS-06
- **Produce:** `tests/Unit/Realtime/ConnectionManagerSendToUserTest.php`, `tests/Unit/Realtime/NotificationServiceTest.php`, `tests/Unit/Realtime/HttpEventBusTest.php`, `tests/Unit/Realtime/MessageHandlerAuthTest.php`, ajuste en `tests/Feature/Realtime/NotificationManagerTest.php`.

## Goal

Cubrir con tests unit las nuevas piezas (sendToUser, NotificationService, HttpEventBus con test server HTTP embebido, MessageHandler rechazando user_id no autenticado) y ajustar el test del NotificationManager para el nuevo event name `notification`. `composer test` verde.

## Pasos

1. `ConnectionManagerSendToUserTest`: sendToUser llega a N conexiones del mismo user; no llega a otras; conexiones sin user no reciben.
2. `NotificationServiceTest`: sendToUser persiste (repo mock) y emite (bus local). Devuelve id/created_at.
3. `HttpEventBusTest`: con un test server PHP embebido (`php -S` en thread background o `stream_socket_server`), `publish` entrega. Apagando el server, `publish` no lanza.
4. `MessageHandlerAuthTest`: conexión sin user_id intenta `private-user.99` → error.
5. `NotificationManagerTest::test_send_dispatches_database_and_realtime`: cambiar `'notification.received'` → `'notification'` (cambio intencional, documentado).

## Output Contract

Devolver: archivos, `composer test` verde, suite completa con el conteo de tests antes/después.
