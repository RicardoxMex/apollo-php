# Handoff — WS-05

## Output Contract

| Campo | Contenido |
|---|---|
| Status | COMPLETED |
| Summary | `apps/Realtime` registrada en `config/apps.php`. `RealtimeApiController` con las rutas del spec §13: `GET /v1/notifications` (filtro `?unread=1`), `GET /v1/notifications/{id}` (404/403), `POST /v1/notifications/{id}/read` (403 si no es del user), `POST /v1/notifications/read-all`, `POST /v1/notifications` (servicio, con `app_secret`). Autorización por dueño en todos los endpoints de notificaciones (`$row['user_id'] === $user->id`). `php apollo route:list` muestra 9 rutas Realtime (34-42 del total 42). |
| Files Changed | `apps/Realtime/Controllers/RealtimeApiController.php`, `apps/Realtime/Routes/api.php`, `config/apps.php` |
| Decisions | D7 del design. La app Realtime se registra por defecto (prefijo `v1`). Las rutas existentes (`events`, `broadcast`, `realtime/auth`, `channels`) se mantienen; se añaden las de notificaciones y la de creación por servicio. |
| Problems | El test `ApiSmokeTest` rompía porque `apps/Uploads/` no existe en disco (pre-existente, no relacionado con WebSockets). Ajustado para tolerar apps ausentes (mismo comportamiento que el binario `apollo`). |
| Risks | Sin cambios. |
| Validation | `php -l` verde en los 3 archivos. `php apollo route:list` muestra 9 rutas Realtime (34-42). `composer test` 182 verde. |
| Next Steps | Consumido por WS-07 (test del controller) y WS-08 (docs). |

## Important Context

- La autorización por dueño usa `(int) $row['user_id'] !== (int) $user->id`. El `User` autenticado viene del middleware `auth` (ApolloAuth) y se expone vía `$request->user()`.
- `POST /v1/notifications` (servicio) requiere `app_secret`; usa `NotificationService::sendToUser` internamente.
- `GET /v1/channels` ahora también devuelve `scheme` (`ws`/`wss`) y `poll_interval` para diagnóstico.
