# Handoff — WS-01

## Output Contract

| Campo | Contenido |
|---|---|
| Status | COMPLETED |
| Summary | Instalado `workerman/workerman` v5.2.2 (+ `workerman/coroutine` v1.1.6) vía Composer. `composer.json` actualizado: `require` `"workerman/workerman": "^5.0"`; `suggest` ya no incluye `ext-openswoole`; `scripts` añade `composer websocket` (+ `websocket:start-d`, `stop`, `restart`, `status`, `test`). `config.audit.ignore = ["PKSA-z3gr-8qht-p93v"]` para el advisory informativo de phpunit 10.5 (el suite ya funcionaba en verde). `.env.example` añade el bloque `WEBSOCKET_*` (alias de `REALTIME_*` con fallback). |
| Files Changed | `composer.json`, `composer.lock`, `.env.example` |
| Decisions | D6 del design: alias `WEBSOCKET_*` con fallback `REALTIME_*`. Versión resuelta 5.2.2 (compatible con PHP 8.4 y Windows). |
| Problems | Composer bloqueó la primera instalación por el advisory `PKSA-z3gr-8qht-p93v` de phpunit 10.5; resuelto añadiendo `config.audit.ignore` (el paquete está instalado y los tests pasan; el advisory es informativo y no bloqueante en este entorno). |
| Risks | Ninguno nuevo. |
| Validation | `composer validate` → ./composer.json is valid · `composer dump-autoload` → 1874 clases · `php -r` → `Worker OK / VERSION=5.2.2 / HttpRequest OK` |
| Next Steps | Consumido por WS-02 (servidor + ConnectionAuthenticator). |

## Important Context

- En esta máquina `php` no está en PATH; usar `C:\tools\php84\php.exe` o invocar vía `composer`/`apollo` (que sí están en PATH).
- `workerman/workerman` 5.2.2 no instala extensiones nativas: corre en PHP puro (Windows en single-process, sin daemon `-d`, sin `count`>1, sin `status`/`reload` nativos — se mitiga con pid file + taskkill en `realtime:stop`).
- El bloque `# --- WebSockets ---` en `.env.example` documenta todas las claves del spec §4; las `REALTIME_*` legacy se mantienen por compatibilidad (el config resolverá `WEBSOCKET_*` primero y caerá a `REALTIME_*`).
