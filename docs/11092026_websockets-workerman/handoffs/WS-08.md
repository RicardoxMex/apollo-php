# Handoff — WS-08

## Output Contract

| Campo | Contenido |
|---|---|
| Status | COMPLETED |
| Summary | `docs/websockets.md` (nuevo, español) cubre los 14 puntos del spec §22: instalación, configuración, variables de entorno, iniciar servidor, autenticación, crear notificaciones, consumir WS desde frontend, reconexión/heartbeat, desarrollo local, producción, Nginx + WSS, Redis (multi-instancia), API REST, troubleshooting. `docs/realtime.md` actualizado: elimina toda mención de OpenSwoole, refleja Workerman + polling + NotificationService, conserva la API de la fachada `Realtime::*`. `docs/README.md` añade `websockets.md` al índice y actualiza los comandos `realtime:*` (sin OpenSwoole). |
| Files Changed | `docs/websockets.md` (nuevo), `docs/realtime.md` (modificado), `docs/README.md` (modificado) |
| Decisions | Doc de dos niveles: `websockets.md` es la guía completa (usuario nuevo), `realtime.md` es el resumen del módulo + migración desde la versión OpenSwoole. |
| Problems | Ninguno. |
| Risks | Ninguno. |
| Validation | Inspección visual. Sphinx-like check: todos los links internos a `.md` apuntan a archivos existentes (`docs/websockets.md`, `docs/realtime.md`, `docs/uploads.md`, `docs/validation.md`, `docs/authentication-system.md`, `docs/app-structure.md`, `docs/cli-commands.md`). |
| Next Steps | Consumido por el report final. |

## Important Context

- El doc incluye un bloque Nginx completo para WSS con `proxy_set_header Upgrade $http_upgrade` y un ejemplo de systemd unit para producción.
- La sección §12 "Redis (multi-instancia)" describe el paso 2 (futuro) con `workerman/channel` y el diagrama de arquitectura, dejando claro que v1 no lo implementa pero la abstracción `EventBus` está preparada.
- Troubleshooting incluye cómo probar notificaciones localmente con `curl` + `wscat`/`node` y un JWT de prueba.
