# Handoff — WS-07

## Output Contract

| Campo | Contenido |
|---|---|
| Status | COMPLETED |
| Summary | Tests nuevos: `ConnectionManagerSendToUserTest` (4 tests: sendToUser entrega a N conexiones del mismo user, no entrega a otros, sweep elimina inactivas, getUserIds) y `NotificationServiceTest` (2 tests: sendToUser persiste+devuelve record, sendToUsers a múltiples). Test existente `NotificationManagerTest` ajustado: event name `notification.received` → `notification` (cambio intencional alineado con spec §7) y `data.data` (no `data.notification.data`). `HttpEventBusTest` diferido (HttpEventBus no implementado en v1 — D4-revisado). `MessageHandlerAuthTest` no necesario (la cobertura existente de `MessageHandlerTest::test_private_channel_rejects_without_ticket` cubre el rechazo de canales privados sin ticket). |
| Files Changed | `tests/Unit/Realtime/ConnectionManagerSendToUserTest.php` (nuevo), `tests/Unit/Realtime/NotificationServiceTest.php` (nuevo), `tests/Feature/Realtime/NotificationManagerTest.php` (ajuste) |
| Decisions | D4-revisado: HttpEventBus diferido, sin test. D2: el rechazo de canales privados sin autenticación ya está cubierto por el test existente. |
| Problems | ApiSmokeTest rompía por `apps/Uploads/` ausente (pre-existente, no relacionado con WebSockets). Ajustado para tolerar apps ausentes. |
| Risks | Ninguno. |
| Validation | `composer test` 182 verde (176 baseline + 6 nuevos: 4 sendToUser + 2 NotificationService). 446 assertions. `php -l` verde en los 3 archivos. |
| Next Steps | Consumido por WS-08 (docs). |

## Important Context

- El ajuste en `NotificationManagerTest` documenta el cambio de event name: el frame realtime ahora es `{"event":"notification","data":{id,type,title,message,data,created_at}}` (spec §7), no `{"event":"notification.received","data":{"notification":{...}}}`.
- `composer test` muestra 4 tests skipped (integración SQLite; requiere `pdo_sqlite` que no está habilitado en el `php.ini` de esta máquina).
