# Bitácora — WebSockets / Workerman

> Registro cronológico del trabajo: cada tarea completada y cada checkpoint se anota aquí (qué, cuándo, evidencia). Formato de checkpoint: `templates/checkpoint.md` (`.ai/templates/checkpoint.md`).

| Fecha | Qué pasó | Evidencia |
|---|---|---|
| 11092026 | Apertura del work folder, spec y design redactados, plan con 8 tareas registrado, plan anterior `10092026_modulo-uploads` archivado en el store. | `docs/11092026_websockets-workerman/{spec,design,plan}.md`, `.ai/state/store/plan.json` |
| 11092026 | **WS-01** COMPLETED: `workerman/workerman` v5.2.2 instalado; `composer.json` con `require ^5.0`, `suggest` sin `ext-openswoole`, `scripts.websocket*`; `.env.example` con bloque `WEBSOCKET_*`. | `composer validate` OK, `Workerman\Worker` v5.2.2 cargable, handoff WS-01 |
| 11092026 | **WS-02** COMPLETED: `WebSocketServer` reescrito sobre Workerman (un solo `Worker` `websocket://`, cross-platform), `ConnectionAuthenticator` (JWT en handshake), `ConnectionManager` +`sendToUser`/`sweep`/`getUserIds`, `MySqlNotificationRepository::forUserAfterId`, `websocket/server.php` (entry). Polling server-side cross-process. End-to-end verificado: `start`→`status`→`HTTP 101`→`AUTH_FAILED`→`stop`. | php -l 11 archivos; handshake real con `Sec-WebSocket-Accept` correcto; handoff WS-02 |
| 11092026 | Revisión D1/D4: HTTP interno en mismo puerto inviable en Windows (`multi workers in one file are not support`); **D1-revisado** (un solo Worker) + **D4-revisado** (polling server-side, `HttpEventBus` diferido). | design.md revisado; spec.md actualizado |
| 11092026 | **WS-03** COMPLETED: `RealtimeConfig` +`enabled`/`publicUrl`/`pollInterval`/`ssl*`; `RealtimeManager` mode-aware (`inProcess=true` default, app-side `false`); `config/realtime.php` con `WEBSOCKET_*` fallback `REALTIME_*`. 176 tests verdes. | php -l verde; composer test verde; handoff WS-03 |
| 11092026 | **WS-04** COMPLETED: `NotificationService::sendToUser($userId, $type, $payload)` (persiste + devuelve registro); `RealtimeChannel` actualizado al frame spec §7 (`event=notification`, `data={id,type,title,message,data,created_at}`); CLI `realtime:{start,stop,restart,status,test}` reescritos cross-platform (taskkill Windows / posix Linux). | php -l 6 archivos; realtime:test reporta Workerman v5.2.2; handoff WS-04 |
| 11092026 | **WS-05** COMPLETED: `apps/Realtime` registrada en `config/apps.php`; 9 rutas REST (prefijo `v1`): `events`/`broadcast`/`realtime.auth`/`channels` (app_secret) + `notifications`(+show)/`read`/`read-all` (JWT, autorización por dueño) + `notifications` POST (servicio). | `php apollo route:list` muestra 9 rutas realtime (34-42 del total 42); handoff WS-05 |
| 11092026 | **WS-06** COMPLETED: `public/js/realtime.js` reescrito — `token` en `?token=<jwt>`, API top-level `on/emit/off/send`, frame `{event, data}` (spec §7), reconexión backoff + heartbeat preservados, API `channel().listen()` por BC. | `node --check` exit 0; handshake `?key=&token=` verificado; handoff WS-06 |
| 11092026 | **WS-07** COMPLETED: `ConnectionManagerSendToUserTest` (4 tests), `NotificationServiceTest` (2 tests), `NotificationManagerTest` ajustado (event name `notification.received`→`notification`). `composer test` 182 verde / 446 assertions. | php -l 3 archivos; composer test 182 OK; handoff WS-07 |
| 11092026 | **WS-08** COMPLETED: `docs/websockets.md` (guía completa 14 puntos spec §22); `docs/realtime.md` actualizado sin OpenSwoole; `docs/README.md` enlaza. | docs escritas y enlazadas; handoff WS-08 |
| 11092026 | **Pipeline completa** (M1+M2+M3, G1–G7 pass): 8/8 tareas COMPLETED, 182 tests verdes, end-to-end `start`→`status`→`handshake 101`→`stop` verificado en Windows, `node --check` verde, `php -l` verde en >150 archivos. | `.ai/state/store/{checkpoints,gates,tasks,handoffs,metrics}.json` actualizados |
| 11092026 | **Refinamiento del usuario**: el WS pertenece al core, no a una app del framework. **WS-09..11** (3 tareas): `apps/Realtime/` eliminado del disco; `config/apps.php` revertido; `docs/websockets.md §13` reescrita como **guía de integración REST** (orientativa, sin rutas hardcodeadas del framework); `docs/realtime.md` y `docs/README.md` actualizados; `spec.md`/`design.md`/`plan.md` coherentes con la nueva arquitectura. Suite 182 verde; handshake 101 OK; `route:list` sin `/v1/` ni `realtime.*`; gates G1–G7 pass. **11/11 tareas COMPLETED**. | `apps/Realtime/` eliminado; `config/apps.php`; `docs/websockets.md` §13 reescrita; `docs/realtime.md`; `docs/README.md`; handoffs WS-09..11 |

